For Windows, use PowerShell.

Setup
Install it:
1. npm install --save next react react-dom
2. npm install --save express
3. npm install --save isomorphic-unfetch
4. npm install --save react-markdown

To run, use:
1. npm run build
2. npm run start

